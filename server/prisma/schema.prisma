datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "cargo prisma"
  output   = "../src/prisma.rs"
}

model Client {
  id  String  @id @unique
  ip  String
  hostname String
  paid Boolean
}

